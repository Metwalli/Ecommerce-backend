import { ProductAttributeLine } from './product-attribute-line.model';

describe('ProductAttributeLine', () => {
  it('should create an instance', () => {
    expect(new ProductAttributeLine()).toBeTruthy();
  });
});
